# SPDX-License-Identifier: Apache-2.0
# Example reverse proxy for backend + frontend (adjust server_name and upstream)
# upstream backend { server backend:8000; }
# upstream frontend { server frontend:3000; }
# server { listen 80; server_name _; location /api/ { proxy_pass http://backend/; } location / { proxy_pass http://frontend/; } }
